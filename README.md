# React 學習筆記：受控組件與狀態管理

## 🚀 任務 1.1.3：DataStore 實作邏輯總結

要在 React 中實現「輸入 -> 儲存 -> 顯示」的邏輯，遵循以下三個核心步驟：

### 1. 初始化狀態 (State)
使用 `useState` 建立記憶體插槽。
```javascript
const [data, setData] = useState("");      // 追蹤輸入框文字
const [saveData, setSaveData] = useState(""); // 追蹤儲存後的文字
```

### 2. 雙向綁定 (Controlled Component)
透過 `value` 與 `onChange` 讓 React 完全掌控輸入框。
- **`value={data}`**: 告訴標籤顯示哪個變數。
- **`onChange`**: 監聽輸入操作，即時更新變數。
```javascript
<input 
  value={data} 
  onChange={(e) => setData(e.target.value)} 
/>
```

### 3. 執行動作 (Trigger Action)
透過事件監聽器（如 `onClick`）來修改狀態，觸發畫面更新。
- **儲存**: `setSaveData(data)` 將臨時文字轉存到儲存區。
- **清空**: `setData("")` 和 `setSaveData("")` 重置所有狀態。

---

**💡 核心觀念**：在 React 裡，你不需要手動操作 DOM。你只需要**更新狀態 (State)**，React 就會自動幫你**更新畫面**。
